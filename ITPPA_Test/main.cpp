/*# include <iostream>
# include <cassert>
using namespace std;

int main() {

	int num;
	cout << "Enter a number" << endl;
	cin >> num;
	cout << endl;
	if (0 <= num && num <= 10) {
		cout << num << " is within 0 and 10" << endl;
	} else {
		cout << num << " is not within 0 and 10" << endl;
	}
	return 0;
} */
// assert (expression) will cache the errors of the expression
// #include <cassert> <> (#define NDEBUG) to disable 
